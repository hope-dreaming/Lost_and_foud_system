export interface LossitemQuery {
    name?: string;
    type?: string;
}

export interface LossitemType {
    name: string;
    type?: string;
    time: string;
    address: string;
    desc?: string;
    uid: string;
}
export interface LossitemLayoutType {
    title: string;

}
